{
  "name": "piscinapro",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "CRM + ERP de vendas, obras e financeiro para empresas de piscinas (vanilla JS + Supabase).",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "vendor": "node scripts/vendor.mjs",
    "lint": "eslint .",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:e2e": "playwright test",
    "check": "npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.63.0",
    "eslint": "10.10.0",
    "globals": "17.12.0"
  },
  "engines": {
    "node": ">=20"
  }
}
